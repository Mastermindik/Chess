import './App.scss'
import StartPage from './pages/startPage/StartPage';

function App() {
  //TODO Game Over

  return (
    <>
      {/* <Suspense fallback={<Loading />} >
        <Outlet />
      </Suspense> */}
      <StartPage />
    </>
  )
}

export default App
